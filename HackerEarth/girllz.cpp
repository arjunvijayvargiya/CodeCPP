#include<bits/stdc++.h>
#define ll long long
using namespace std;
int main(int argc, char** argv)
{
   ll m,n;
   cin>>m;
   ll arr[101];
   memset(arr,0,sizeof(arr));
   for(ll i=0;i<m;i++)
   {ll x;
       cin>>x;
       arr[x]=1;
   }
   cin>>n;
   for(ll i=0;i<n;i++)
   {
       ll buf[101];
       string str;
       cin>>str;
       ll bun[3];
       for(ll j=0;str[j]!='\0';j++)
       {
           if(is)

       }
   }
    return 0;
}
